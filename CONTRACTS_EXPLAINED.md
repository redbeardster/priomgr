# Детальный разбор контрактов Kani и Prusti

## 📋 Обзор

Давайте детально разберём каждый контракт и proof, что именно они проверяют и почему это важно.

---

## 🟣 Prusti Контракты

### Что такое контракты?

**Контракты** - это формальные спецификации поведения функции:
- **`requires`** (предусловие) - что должно быть истинно **перед** вызовом
- **`ensures`** (постусловие) - что гарантируется **после** вызова
- **`pure`** - функция без побочных эффектов

### Контракт 1: calculate_optimal_priority_verified

```rust
#[requires(MIN_PRIORITY <= current_priority && current_priority <= MAX_PRIORITY)]
#[ensures(MIN_PRIORITY <= result && result <= MAX_PRIORITY)]
#[ensures(load > 50.0 ==> result <= current_priority)]
pub fn calculate_optimal_priority_verified(...) -> i32
```

#### Разбор по частям

**Предусловие (requires):**
```rust
MIN_PRIORITY <= current_priority && current_priority <= MAX_PRIORITY
```

**Что проверяет:**
- Входной приоритет должен быть в границах [10, 95]
- Вызывающий код **обязан** гарантировать это

**Почему важно:**
- Если входное значение невалидно, функция может работать некорректно
- Это **контракт** между вызывающим кодом и функцией

**Пример нарушения:**
```rust
// ❌ ОШИБКА: Prusti выдаст ошибку
calculate_optimal_priority_verified(5, 50, 0.0, 0, 8192, 4);
//                                   ^ 5 < MIN_PRIORITY (10)

// ✅ OK
calculate_optimal_priority_verified(50, 50, 0.0, 0, 8192, 4);
//                                   ^ 50 в границах [10, 95]
```

---

**Постусловие 1 (ensures):**
```rust
MIN_PRIORITY <= result && result <= MAX_PRIORITY
```

**Что проверяет:**
- Результат **всегда** в границах [10, 95]
- Независимо от входных параметров (при соблюдении предусловий)

**Почему важно:**
- Гарантирует, что функция не вернёт невалидный приоритет
- Вызывающий код может **полагаться** на это

**Как Prusti проверяет:**
```rust
// Prusti анализирует все пути выполнения:

if load > threshold {
    priority = (priority - 10).max(MIN_PRIORITY);  // ✓ >= MIN_PRIORITY
}

if responsiveness > 2000 {
    priority = (priority - 5).max(MIN_PRIORITY);   // ✓ >= MIN_PRIORITY
}

priority.clamp(MIN_PRIORITY, MAX_PRIORITY)         // ✓ в [MIN, MAX]
```

Prusti **доказывает**, что на каждом пути результат в границах.

---

**Постусловие 2 (ensures):**
```rust
load > 50.0 ==> result <= current_priority
```

**Что проверяет:**
- **Импликация**: ЕСЛИ нагрузка > 50, ТО результат ≤ входного приоритета
- Это свойство **монотонности** - при высокой нагрузке приоритет не увеличивается

**Почему важно:**
- Гарантирует, что система не усугубит перегрузку
- Соответствует нашей TLA+ спецификации

**Символ `==>`:**
- Это **импликация** в логике
- `A ==> B` означает "если A истинно, то B должно быть истинно"
- Если A ложно, импликация всегда истинна (vacuous truth)

**Примеры:**
```rust
// load = 60.0 (> 50.0)
// current_priority = 80
// result = 70 (после корректировки)
// 60.0 > 50.0 ==> 70 <= 80  ✓ истинно

// load = 30.0 (< 50.0)
// current_priority = 80
// result = 85 (гипотетически)
// 30.0 > 50.0 ==> 85 <= 80  ✓ истинно (vacuously, т.к. 30 < 50)
```

---

### Контракт 2: apply_priority_adjustment

```rust
#[requires(MIN_PRIORITY <= old_priority && old_priority <= MAX_PRIORITY)]
#[requires(step > 0)]
#[ensures(MIN_PRIORITY <= result && result <= MAX_PRIORITY)]
#[ensures(result <= old_priority)]
pub fn apply_priority_adjustment(old_priority: i32, step: i32) -> i32
```

#### Разбор

**Предусловия:**
```rust
MIN_PRIORITY <= old_priority && old_priority <= MAX_PRIORITY
step > 0
```

**Что проверяет:**
1. Старый приоритет валиден
2. Шаг положительный (корректировка вниз)

**Постусловия:**
```rust
MIN_PRIORITY <= result && result <= MAX_PRIORITY
result <= old_priority
```

**Что гарантирует:**
1. Результат в границах
2. Результат не больше старого значения (монотонность)

**Реализация:**
```rust
(old_priority - step).max(MIN_PRIORITY)
```

**Как Prusti доказывает:**
```
Дано: 10 <= old_priority <= 95, step > 0

Случай 1: old_priority - step >= 10
  result = old_priority - step
  ✓ result >= 10 (по условию)
  ✓ result <= 95 (т.к. result < old_priority <= 95)
  ✓ result <= old_priority (т.к. step > 0)

Случай 2: old_priority - step < 10
  result = 10
  ✓ result >= 10 (по определению)
  ✓ result <= 95 (т.к. 10 < 95)
  ✓ result <= old_priority (т.к. old_priority >= 10)

QED
```

---

### Контракт 3: needs_adjustment

```rust
#[pure]
pub fn needs_adjustment(load: f64, threshold: f64, priority: i32) -> bool
```

**Что такое `#[pure]`:**
- Функция **без побочных эффектов**
- Не изменяет глобальное состояние
- Всегда возвращает одинаковый результат для одинаковых входов
- Может использоваться в **спецификациях** других функций

**Почему важно:**
```rust
// Можно использовать в контрактах:
#[requires(needs_adjustment(load, 50.0, priority))]
#[ensures(!needs_adjustment(load, 50.0, result))]
pub fn adjust_if_needed(priority: i32, load: f64) -> i32 {
    // ...
}
```

**Пример использования:**
```rust
// В спецификации
#[ensures(needs_adjustment(load, threshold, old_priority) ==> 
          result < old_priority)]
pub fn conditional_adjust(...) -> i32 {
    if needs_adjustment(load, threshold, priority) {
        adjust(priority)
    } else {
        priority
    }
}
```

---

## 🟢 Kani Proofs

### Что такое Kani proof?

**Kani proof** - это функция, которая:
1. Создаёт **символьные** (любые возможные) значения
2. Устанавливает **предусловия** через `kani::assume`
3. Вызывает проверяемую функцию
4. Проверяет **постусловия** через `assert!`

Kani проверяет **все возможные пути выполнения** (bounded).

---

### Proof 1: verify_priority_bounds

```rust
#[kani::proof]
fn verify_priority_bounds() {
    let current_priority: i32 = kani::any();
    let target_priority: i32 = kani::any();
    let load: f64 = kani::any();
    // ...
    
    kani::assume(MIN_PRIORITY <= current_priority && current_priority <= MAX_PRIORITY);
    kani::assume(MIN_PRIORITY <= target_priority && target_priority <= MAX_PRIORITY);
    kani::assume(load >= 0.0 && load <= 100.0);
    // ...
    
    let result = calculate_optimal_priority_verified(...);
    
    assert!(result >= MIN_PRIORITY);
    assert!(result <= MAX_PRIORITY);
}
```

#### Детальный разбор

**Шаг 1: Символьные значения**
```rust
let current_priority: i32 = kani::any();
```

**Что это значит:**
- `kani::any()` создаёт **символьное** значение
- Это не конкретное число, а **любое возможное** значение типа `i32`
- Kani будет проверять **все** возможные значения

**Аналогия:**
```
Обычный тест:  current_priority = 50  (одно значение)
Kani proof:    current_priority = ?   (все значения i32)
```

---

**Шаг 2: Предусловия (assume)**
```rust
kani::assume(MIN_PRIORITY <= current_priority && current_priority <= MAX_PRIORITY);
```

**Что это делает:**
- **Ограничивает** пространство проверки
- Говорит Kani: "рассматривай только значения в [10, 95]"
- Это моделирует **предусловие** функции

**Без assume:**
```
Kani проверит: -2147483648 .. 2147483647 (все i32)
Время: очень долго
```

**С assume:**
```
Kani проверит: 10 .. 95
Время: разумное
```

---

**Шаг 3: Вызов функции**
```rust
let result = calculate_optimal_priority_verified(
    current_priority,
    target_priority,
    load,
    responsiveness,
    memory,
    cpus,
);
```

**Что происходит:**
- Kani **символьно выполняет** функцию
- Отслеживает все возможные пути
- Строит формулу для каждого пути

---

**Шаг 4: Постусловия (assert)**
```rust
assert!(result >= MIN_PRIORITY);
assert!(result <= MAX_PRIORITY);
```

**Что проверяет:**
- Для **всех** возможных входных значений (в границах assume)
- Результат **всегда** в [10, 95]

**Как Kani проверяет:**
```
1. Генерирует SAT формулу для всех путей
2. Передаёт в CBMC (C Bounded Model Checker)
3. CBMC использует SAT solver (MiniSat/Z3)
4. Если найден контрпример - выдаёт ошибку
5. Если контрпримера нет - proof успешен
```

---

### Proof 2: verify_monotonic_decrease_under_load

```rust
#[kani::proof]
fn verify_monotonic_decrease_under_load() {
    let current_priority: i32 = kani::any();
    let target_priority: i32 = kani::any();
    let load: f64 = kani::any();
    let cpus: usize = kani::any();

    kani::assume(MIN_PRIORITY <= current_priority && current_priority <= MAX_PRIORITY);
    kani::assume(MIN_PRIORITY <= target_priority && target_priority <= MAX_PRIORITY);
    kani::assume(cpus > 0 && cpus <= 128);
    kani::assume(load > (cpus as f64) * CRITICAL_LOAD_MULTIPLIER as f64);
    //           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    //           КЛЮЧЕВОЕ УСЛОВИЕ: высокая нагрузка

    let result = calculate_optimal_priority_verified(
        current_priority,
        target_priority,
        load,
        0,
        8192,
        cpus,
    );

    assert!(result <= target_priority);
    //      ^^^^^^^^^^^^^^^^^^^^^^^^
    //      МОНОТОННОСТЬ: не увеличивается
}
```

#### Что проверяет

**Свойство монотонности:**
```
∀ priority, load, cpus:
  (load > cpus * 2.0) ⇒ adjust(priority, load) ≤ priority
```

**В коде:**
```rust
if load > (num_cpus as f64) * CRITICAL_LOAD_MULTIPLIER as f64 {
    priority = (priority - 10).max(MIN_PRIORITY);
    //         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    //         Всегда <= исходного priority
}
```

**Почему важно:**
- Гарантирует, что при перегрузке система **не усугубит** ситуацию
- Соответствует теореме из Isabelle:
  ```isabelle
  theorem priority_decreases_under_load:
    assumes "load > CRITICAL_LOAD"
    shows "adjust(priority) ≤ priority"
  ```

---

### Proof 3: verify_adjustment_decreases

```rust
#[kani::proof]
fn verify_adjustment_decreases() {
    let old_priority: i32 = kani::any();
    let step: i32 = kani::any();

    kani::assume(MIN_PRIORITY <= old_priority && old_priority <= MAX_PRIORITY);
    kani::assume(step > 0 && step <= 100);

    let result = apply_priority_adjustment(old_priority, step);

    assert!(result <= old_priority);
    assert!(result >= MIN_PRIORITY);
}
```

#### Что проверяет

**Два свойства:**

1. **Монотонность:** `result ≤ old_priority`
   ```
   Корректировка никогда не увеличивает приоритет
   ```

2. **Нижняя граница:** `result ≥ MIN_PRIORITY`
   ```
   Приоритет не может упасть ниже минимума
   ```

**Все возможные случаи:**
```rust
// Случай 1: old_priority = 50, step = 10
// result = max(40, 10) = 40
// ✓ 40 <= 50
// ✓ 40 >= 10

// Случай 2: old_priority = 15, step = 10
// result = max(5, 10) = 10
// ✓ 10 <= 15
// ✓ 10 >= 10

// Случай 3: old_priority = 10, step = 100
// result = max(-90, 10) = 10
// ✓ 10 <= 10
// ✓ 10 >= 10
```

Kani проверяет **все** эти случаи автоматически!

---

### Proof 4: verify_finite_adjustments

```rust
#[kani::proof]
#[kani::unwind(10)]  // ← ВАЖНО!
fn verify_finite_adjustments() {
    let mut priority: i32 = kani::any();
    kani::assume(MIN_PRIORITY <= priority && priority <= MAX_PRIORITY);

    let mut count = 0;
    while priority > MIN_PRIORITY && count < 10 {
        priority = apply_priority_adjustment(priority, 10);
        count += 1;
    }

    assert!(count <= 9 || priority == MIN_PRIORITY);
}
```

#### Детальный разбор

**Атрибут `#[kani::unwind(10)]`:**
```rust
#[kani::unwind(10)]
```

**Что это значит:**
- Kani **развернёт** цикл максимум 10 раз
- Это **bounded** model checking - проверяем до определённой глубины
- Без этого Kani не может проверить бесконечные циклы

**Почему 10:**
```
Максимальный приоритет: 95
Минимальный приоритет: 10
Шаг корректировки: 10

Количество итераций: ⌈(95 - 10) / 10⌉ = ⌈8.5⌉ = 9

Берём 10 для запаса
```

---

**Что проверяет:**
```
После максимум 9 корректировок приоритет достигнет минимума
```

**Трасса выполнения:**
```
Итерация 0: priority = 95, count = 0
Итерация 1: priority = 85, count = 1  (95 - 10)
Итерация 2: priority = 75, count = 2  (85 - 10)
Итерация 3: priority = 65, count = 3  (75 - 10)
Итерация 4: priority = 55, count = 4  (65 - 10)
Итерация 5: priority = 45, count = 5  (55 - 10)
Итерация 6: priority = 35, count = 6  (45 - 10)
Итерация 7: priority = 25, count = 7  (35 - 10)
Итерация 8: priority = 15, count = 8  (25 - 10)
Итерация 9: priority = 10, count = 9  (15 - 10, но max(5, 10) = 10)
Выход: priority = 10, count = 9

✓ count <= 9 ИЛИ priority == MIN_PRIORITY
✓ 9 <= 9 ✓
```

**Связь с Isabelle:**
```isabelle
theorem finite_adjustments:
  assumes "priorities_in_bounds s"
  shows "adjustments s ≤ (MAX_PRIORITY - MIN_PRIORITY) div 10 + 1"
```

Kani **проверяет** это свойство на конкретном коде!

---

### Proof 5: verify_clamp_bounds

```rust
#[kani::proof]
fn verify_clamp_bounds() {
    let value: i32 = kani::any();
    let result = value.clamp(MIN_PRIORITY, MAX_PRIORITY);
    
    assert!(result >= MIN_PRIORITY);
    assert!(result <= MAX_PRIORITY);
}
```

#### Что проверяет

**Корректность `clamp`:**
```rust
value.clamp(min, max)
```

**Все случаи:**
```
Случай 1: value < MIN_PRIORITY (например, 5)
  result = MIN_PRIORITY = 10
  ✓ 10 >= 10
  ✓ 10 <= 95

Случай 2: MIN_PRIORITY <= value <= MAX_PRIORITY (например, 50)
  result = value = 50
  ✓ 50 >= 10
  ✓ 50 <= 95

Случай 3: value > MAX_PRIORITY (например, 100)
  result = MAX_PRIORITY = 95
  ✓ 95 >= 10
  ✓ 95 <= 95
```

**Почему проверяем стандартную функцию:**
- Убеждаемся, что понимаем её поведение правильно
- Проверяем, что константы корректны
- Документируем ожидаемое поведение

---

## 🔄 Связь между Prusti и Kani

### Prusti контракт → Kani proof

**Prusti:**
```rust
#[requires(MIN_PRIORITY <= old_priority && old_priority <= MAX_PRIORITY)]
#[ensures(result <= old_priority)]
pub fn apply_priority_adjustment(old_priority: i32, step: i32) -> i32
```

**Соответствующий Kani proof:**
```rust
#[kani::proof]
fn verify_adjustment_decreases() {
    let old_priority: i32 = kani::any();
    let step: i32 = kani::any();
    
    // requires → assume
    kani::assume(MIN_PRIORITY <= old_priority && old_priority <= MAX_PRIORITY);
    
    let result = apply_priority_adjustment(old_priority, step);
    
    // ensures → assert
    assert!(result <= old_priority);
}
```

**Разница:**
- **Prusti** - дедуктивная верификация, доказывает математически
- **Kani** - bounded model checking, проверяет все пути до глубины N

---

## 📊 Сравнение методов проверки

| Аспект | Prusti | Kani |
|--------|--------|------|
| **Подход** | Дедуктивный | Model checking |
| **Полнота** | Полная (для контрактов) | Bounded |
| **Автоматизация** | Частичная | Полная |
| **Циклы** | Требуют инварианты | Требуют unwind |
| **Скорость** | Быстро (~5s) | Средне (~15s) |
| **Выразительность** | Высокая | Средняя |
| **Зрелость** | Экспериментальная | Стабильная |

---

## 💡 Практические рекомендации

### Когда использовать Prusti

✅ **Хорошо для:**
- Функций с чёткими контрактами
- Композиции функций
- Документирования предусловий
- Проверки инвариантов

❌ **Плохо для:**
- Async кода
- Unsafe кода
- Сложных циклов
- Системных вызовов

### Когда использовать Kani

✅ **Хорошо для:**
- Проверки граничных случаев
- Функций без async
- Алгоритмов с циклами (с unwind)
- Поиска переполнений

❌ **Плохо для:**
- Async кода
- Очень больших функций
- Бесконечных циклов
- FFI

---

## 🎯 Что мы доказали

### Через Prusti контракты

1. ✅ **Безопасность границ** - приоритеты всегда в [10, 95]
2. ✅ **Монотонность** - при высокой нагрузке не увеличиваются
3. ✅ **Корректность корректировки** - всегда уменьшает или сохраняет

### Через Kani proofs

1. ✅ **Границы для всех входов** - проверено символьно
2. ✅ **Монотонность при нагрузке** - для всех значений load
3. ✅ **Корректность adjustment** - для всех priority и step
4. ✅ **Конечность** - максимум 9 итераций
5. ✅ **Корректность clamp** - для всех значений

---

## 🚀 Следующие шаги

### Расширение контрактов

```rust
// Добавить более сложные контракты
#[requires(target_priority >= MIN_PRIORITY)]
#[requires(target_priority <= MAX_PRIORITY)]
#[ensures(result >= target_priority - 18)]  // Максимальное снижение: 10+5+3
#[ensures(load > 50.0 && responsiveness > 2000 && memory < 100 ==> 
          result == max(MIN_PRIORITY, target_priority - 18))]
pub fn calculate_optimal_priority_verified(...) -> i32
```

### Добавление инвариантов циклов

```rust
#[requires(n >= 0)]
#[ensures(result == n * (n + 1) / 2)]
pub fn sum_to_n(n: i32) -> i32 {
    let mut sum = 0;
    let mut i = 0;
    
    #[invariant(sum == i * (i + 1) / 2)]  // ← Инвариант цикла
    #[invariant(i <= n)]
    while i < n {
        i += 1;
        sum += i;
    }
    
    sum
}
```

---

## 📚 Ресурсы

- [Kani Book](https://model-checking.github.io/kani/)
- [Prusti User Guide](https://viperproject.github.io/prusti-dev/user-guide/)
- [Design by Contract](https://en.wikipedia.org/wiki/Design_by_contract)
- [Hoare Logic](https://en.wikipedia.org/wiki/Hoare_logic)

---

**Итог:** Контракты и proofs дают нам **математические гарантии** корректности кода, дополняя TLA+ и Isabelle верификацию на уровне реализации! 🎉
